package assignment2;

public class boolean isSymmetric(TreeNode root) {
        TreeNode left=
        if(sameNode(root.left, root.right)){
        return sameNode()
        }
        }

private boolean sameNode(TreeNode first, TreeNode second){
        if(first.val=second.val){
        if(first.left!=null && second.right!=null){

        }
        return sameNode(first.);
        }
        return false;
        }